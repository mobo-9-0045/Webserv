server:
	location /exampl:
		root /valid_path_file
		index /(default)
		upload /path
	location /ok:
		root /valid_path_file1
		index /(default)1
		upload /path1
	location /ok2:
		root /valid_path_file2
		index /(default)2
		upload /path2
